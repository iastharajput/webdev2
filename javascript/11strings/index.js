// const name = 'John Doe';
// repocount = 5;  

// console.log = (`Hello, my name is ${name} and I have ${repocount} repositories.`);

const game = 'The Legend of Zelda: Breath of the Wild';
console.log(game.length); // Outputs the length of the string
console.log(game.toUpperCase()); // Converts the string to uppercase
console.log(game.toLowerCase()); // Converts the string to lowercase
console.log(game.indexOf('Zelda')); // Finds the index of 'Zelda' in the string
console.log(game.slice(4, 9)); // Extracts a substring from index 4 to 9
console.log(game.replace('Breath of the Wild', 'Tears of the Kingdom')); // Replaces part of the string
